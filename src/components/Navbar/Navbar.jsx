import "./Navbar.scss";

const Navbar = () => {
  return (
    <nav>
      <h1>Gravity</h1>
      <div>
        <span className="hamburger__bar"></span>
        <span className="hamburger__bar"></span>
      </div>
    </nav>
  );
};

export default Navbar;
